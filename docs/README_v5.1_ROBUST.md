# Dialog Analyzer v5.1 - ROBUST PARSING

## 🆕 Что нового в v5.1?

### ✅ Надёжный парсинг JSONL
Восстановлена **robust логика парсинга** из v3.5.5:

1. **Обработка "Extra data" ошибок**
   - Файлы с несколькими JSON на одной строке
   - Автоматическое извлечение через `json.JSONDecoder().raw_decode()`

2. **Подробная статистика**
   - Количество успешно загруженных
   - Исправленных через raw_decode
   - Пропущенных (пустые, невалидные)
   - Обработанных строк

3. **Улучшенная диагностика**
   - Показ первых 10 ошибок парсинга
   - Детальная статистика по типам ошибок
   - Процентное распределение типов интентов

## 🚀 Быстрый старт

### Обновление существующего проекта

```bash
cd C:\\Users\\karenkikh\\Desktop\\json2mermaid-main

# 1. Создайте бэкап
copy utils\\loaders.py utils\\loaders.py.bak

# 2. Скачайте новый файл из репозитория
# Или скопируйте вручную содержимое utils/loaders.py

# 3. Запустите анализ
python generator_v5.0_main.py
```

### Что изменится?

**ДО (v5.0):**
```
⚠️  Line 1: JSON decode error - Expecting value: line 1 column 2 (char 1)
⚠️  Line 2: JSON decode error - Extra data: line 1 column 12 (char 11)
❌ Error loading file: 'str' object has no attribute 'get'
❌ Нет данных для анализа
```

**ПОСЛЕ (v5.1):**
```
✅ Successfully loaded: 245 records
🔧 Fixed (Extra data): 67 records
⚪ Skipped (empty/comments): 15 lines
⚠️  Skipped (invalid JSON): 8 lines
📝 Total lines processed: 335

📊 Статистика загрузки
✅ Всего загружено: 312 интентов
📝 Обработано строк: 335
```

## 📋 Поддерживаемые форматы

### 1. Стандартный JSONL (одна строка = один JSON)
```jsonl
{"intent_id": "1", "title": "Test 1"}
{"intent_id": "2", "title": "Test 2"}
```

### 2. Extra data (несколько JSON на строке)
```jsonl
{"id": "1"} {"id": "2"}
```
✅ **v5.1 обработает оба объекта!**

### 3. JSON массив (весь файл)
```json
[
  {"intent_id": "1", "title": "Test 1"},
  {"intent_id": "2", "title": "Test 2"}
]
```

### 4. Пустые строки и комментарии
```jsonl
# Комментарий
{"intent_id": "1"}

// Ещё комментарий
{"intent_id": "2"}
```
✅ **Пропускаются автоматически**

## 🔧 Технические детали

### Алгоритм парсинга

1. **Попытка 1**: Загрузка как JSON массив (`json.load()`)
   - Весь файл целиком
   - Быстро для маленьких файлов

2. **Попытка 2**: JSONL построчно
   - Стандартный `json.loads(line)`
   - При ошибке → raw_decode для извлечения

3. **Попытка 3**: raw_decode loop
   - Извлекает все JSON из строки
   - Обрабатывает "Extra data"

### Пример кода

```python
try:
    obj = json.loads(line)
    intents.append(obj)
except json.JSONDecodeError:
    # ROBUST: Извлекаем через raw_decode
    decoder = json.JSONDecoder()
    remaining = line
    
    while remaining:
        try:
            obj, idx = decoder.raw_decode(remaining)
            intents.append(obj)
            remaining = remaining[idx:]
        except:
            break
```

## 📊 Статистика производительности

| Метрика | v5.0 | v5.1 ROBUST |
|---------|------|-------------|
| Обработка "Extra data" | ❌ | ✅ |
| Множественные объекты | ❌ | ✅ |
| Статистика ошибок | Базовая | Детальная |
| Диагностика | Минимальная | Полная |

## 🐛 Решение проблем

### Ошибка: "Нет данных для анализа"

**Причина**: Все строки невалидный JSON

**Решение**:
```bash
# Проверьте формат файла
head -n 5 intent_data.jsonl

# Попробуйте один из примеров:
python -c "import json; print(json.dumps({'intent_id': 'test', 'title': 'Test'}))" > test.jsonl
```

### Много "Skipped (invalid JSON)"

**Причина**: Файл не в формате JSON/JSONL

**Решение**:
1. Проверьте кодировку (должна быть UTF-8)
2. Убедитесь что это JSON, а не Python dict
   - ❌ `{'key': 'value'}` (одинарные кавычки)
   - ✅ `{"key": "value"}` (двойные кавычки)

---

**Версия**: 5.1 ROBUST PARSING  
**Дата**: 2026-01-16  
**Совместимость**: Python 3.7+
