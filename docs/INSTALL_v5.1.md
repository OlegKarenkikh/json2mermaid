# 🚀 Инструкция по обновлению до v5.1 ROBUST

## Метод 1: Быстрое обновление (Рекомендуется)

```powershell
# 1. Откройте PowerShell в папке проекта
cd C:\\Users\\karenkikh\\Desktop\\json2mermaid-main

# 2. Создайте бэкап (на всякий случай)
copy utils\\loaders.py utils\\loaders.py.bak

# 3. Скачайте новый файл из GitHub
# https://github.com/OlegKarenkikh/json2mermaid
# Скопируйте utils/loaders.py в вашу папку

# 4. Запустите
python generator_v5.0_main.py
```

## Метод 2: Git pull (если используете Git)

```bash
cd C:\\Users\\karenkikh\\Desktop\\json2mermaid-main
git pull origin main
python generator_v5.0_main.py
```

## ✅ Проверка установки

После запуска вы должны увидеть:

```
================================================================================
🚀 DIALOG ANALYZER v5.1 - ROBUST PARSING EDITION
================================================================================
📜 Режим: Read-Only Analysis with Robust JSONL Parsing
🛡️  Данные не изменяются - только визуализация и метрики
🔧 НОВОЕ: Обработка невалидного JSONL (Extra data, multiple objects)

================================================================================
📅 ЭТАП 1: Загрузка данных
================================================================================
📂 Loading data from: intent_data.jsonl
   Size: 125,456 bytes (0.12 MB)
✅ Successfully loaded: 245 records
🔧 Fixed (Extra data): 67 records
📝 Total lines processed: 335
```

## 🐛 Если не работает

### Ошибка: "ModuleNotFoundError"

```bash
# Убедитесь что вы в правильной папке
cd C:\\Users\\karenkikh\\Desktop\\json2mermaid-main

# Проверьте структуру
dir utils
# Должны быть: loaders.py, config.py, validators.py, analyzers.py
```

### Всё ещё "Extra data" ошибки?

```bash
# Проверьте что изменения сохранены
python -c "from utils.loaders import load_intents; import inspect; print(len(inspect.getsource(load_intents)))"
# Должно быть > 3000 (длина новой функции)
```

## 📦 Альтернатива: Ручная установка

1. Откройте `utils/loaders.py` в текстовом редакторе
2. Найдите функцию `load_intents()`
3. Замените её содержимое на код из GitHub
4. Сохраните файл
5. Запустите `python generator_v5.0_main.py`

---

**Нужна помощь?** Создайте issue на GitHub с выводом команды и первыми 5 строками вашего файла.
